{"name":"tmdb-java","tagline":"A Java wrapper around The Movie Database API v3. It is still incomplete, pull requests welcome.","body":"### tmdb-java\r\nA still incomplete Java wrapper around the [TMDb v3 API](http://docs.themoviedb.apiary.io/), based upon [JakeWharton/trakt-java/](https://github.com/JakeWharton/trakt-java/).\r\n\r\n**Pull requests (e.g. support for more API endpoints, bugfixes) against dev are welcome!**\r\n\r\n## Usage\r\n    \r\n    // Create an instance of ServiceManager once\r\n    ServiceManager manager = new ServiceManager();\r\n    manager.setReadTimeout(10000);\r\n    manager.setConnectionTimeout(15000);\r\n    manager.setApiKey(\"yourapikey\");\r\n    \r\n    // Build your query\r\n    Movie movie = manager.moviesService().summary(123).fire();\r\n\r\n## Using with Android\r\nBecause some device manufacturers shipped a non-hidden version of GSON you **should** replace the required GSON jar with a version with the package name customized, to e.g. myjson. For details see this [GSON help page][3] and the myjson branch (especially the `libs` folder) for an example on how to do this.","google":"","note":"Don't delete this file! It's used internally to help with page regeneration."}